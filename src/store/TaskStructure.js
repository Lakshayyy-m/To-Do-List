class TaskStructure {
  constructor(title, date, description, tasks, id) {
    this.id = id;
    this.title = title;
    this.date = date;
    this.description = description;
    this.tasks = tasks;
  }
}

export default TaskStructure;
